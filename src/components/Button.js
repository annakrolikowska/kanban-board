
import React from 'react';
import { StyledButton } from '../styles/Button.styled';


const Button = props => {
    
    return (
        <StyledButton>
        + Add task 
        </StyledButton>
    );
}

export default Button;